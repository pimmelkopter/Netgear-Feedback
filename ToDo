✅ - Aufräumen
    - Setup Scripte trennen und in eigenen Ordner    - utils verschlanken - so dass ein generator alle logiken kann
✅ - zwei unterschiedliche Tempi für schleifen
✅ - Port stats sind umbenannt
TODO - Wenn link nicht aktiv beide ports vlan 
✅ echte response zu port stats anschauen, was zeigt link power - bei single led wechsel zwischen vlan und status
✅ blink aktuell nur mit Update intervall
✅ - POE Status integrieren
✅ - Link status
TODO OBB -> geht doch über fixed? testen!
TODO cfg raussuchen für VLAN NAMEN

TODO - mehr in journal schreiben oder python info und echo print

TODO - anderer Networkmanager auch integrieren

OOB ja viel praktischer
  "oob": false,
  "oob_ip": "192.168.0.239",
  "oob_fixed_ip": "192.168.0.1/24",
  "oob_gw": "192.168.0.239",

TODO GUI Webinterface - Flask/Bottle
    Wifi random - ohne shared sondern ipv4 manual
    Anzeige der VLANs und Settings
    Änderung der VLANs
    Save/ Save&Reboot
    Passwortschutz
    Wifi bis nächstem Boot deaktivieren
    Print/ Echo im Interface anzeigen
    wenn mehrere Switche im Netz auffindbar? - in Web-GUI Auswahl treffen

TODO - Portierung auf Pi Zero
TODO - Portierung auf ESP32
    - erst mit einer LED
    Dann LAN
    dann api test
    - ESP32 main.py Generator aus einer config.json?
    - OTA Update für ESP32?


Ideen/ checken:
- UI mit LED-Buttons + TFT + Push/Turn-Encoder
- update switch - kann man ssl handshake safe machen? sonst an Netgear schreiben

Notizen
pi4.local 
192.168.0.1
pi raspberry

Commit:

git commit -a -m "Big Update" -m "Port Abfrage funktioniert und wird auf 8/16/24/40 gemappt" -m "VLANs werden abgerufen und in Config geschrieben" -m "es gibt nur noch eine HTTP Abfrage für Port VLAN/Status -ALL - also alle Ports gleichzeitig"


ERLEDIGT:
✅ sudo systemctl restart NetworkManager / sudo reboot nach Network-Changes?
✅ IP-Scan Fortschritt anzeigen
✅ - Mapping schöner, evtl bessere Ansätze mit ein paar vorkonfigurierten
✅ - Port Mapping Lücken für die 6er-Blöcke
✅ Script läuft mit Mockserver
✅ Festen Wifi Hotspot erstellen, der einen SSH Zugriff ermöglicht, auch wenn man für die Switch-Erkennung in einem anderen Subnetz ist. Also zum Beispiel "Test-AP-RPI4" pw "netgear12345678" mit IP Adresse 192.168.0.1 und aktiviertem DHCP und SSH
✅ Netzwerksettings überarbeiten in Config.json und setup.sh und update.sh damit sie den neuen Netzwerkmanager übernehmen (siehe Infos unten)
✅Pingtest mit echtem Switch testen - Überprüfung über Wifi und SSH ob Switch im Subnetz gefunden wird
✅ LOGIN API!
✅ Meine tatsächlichen VLAN IDs und Farben in meine config übernehmen, dann Test ob das funktioniert wie  gedacht
✅PORT Mapping - wenn Ports nicht definiert sind was passiert?
✅- Fehlercodes über LEDs